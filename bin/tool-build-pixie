#!/bin/bash -e

if [[ ! -d ~/code/pixie ]];then
    git clone https://github.com/pixie-lang/pixie ~/code/pixie
    cd ~/code/pixie
else
    cd ~/code/pixie
    git pull --ff-only
fi

# dep for osx -brew install boost

make build_with_jit
ln -s $PWD/pixie-vm ~/toolkit/bin/pxi
